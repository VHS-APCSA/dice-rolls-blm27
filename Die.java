public class Die 
{
	public static int die(int sides)
	{
		return (int)(Math.random() *sides) + 1;
	}
	/**
	 * Write a single static method that takes an int parameter representing
	 * the number of sides on a fair die and returns a random int
	 * between 1 and the parameter inclusive.
	 * @param An int that represents the number of sides on a fair die.
	 * @return A random int between 1 and the parameter inclusive.
	 */
}
